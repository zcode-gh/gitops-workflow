name: Pre-commit Hooks Check

on:
  pull_request:
    branches:
      - main

jobs:
  pre-commit-check:
    name: Run Pre-commit Hooks
    uses: zcode-gh/gitops-workflow/.github/workflows/pre-commit.yml@main
